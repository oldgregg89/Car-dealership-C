@using CarDealership.Models;

<h1>Car Dealership</h1>

@if (Model.Count == 0)
{
  <p>There are no Cars in the list.</p>
}
<ul>
  @foreach (Car item in Model)
  {
    <li>@item.MakeModel, $@item.Price, @item.Miles miles</li>
  }

</ul>
<a href="/Cars/new">Add a new Car.</a>